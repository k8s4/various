# Some shit notes


sort()
len()
set()

range(0,10,1)


#debug:
type()
sys.getsizeof()
len()


str.index('char', 2, 16)
list() append, insert, extend, pop, remove

str() strip, lstrip, rstrip, ljust, rjust, split, join, upper, capitalize, title, swapcase, lower
str() startwith, endwith, isalnum, isalpha, isnumeric, istitle, islower, isupper
dict() keys, values, get, del

sorted()

# fileee - r, b - binary, w, a - append
c = open(filepath, 'type') - like string into memory
c.read(), close(), readlines(), write()

with open(path, 'r') as c:
  pass

