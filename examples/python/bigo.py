# O(1) constant

# O(log2n) logarithm

# O(n) linear, recurse function
def sum(a):
  if a == 1: 
    return 1
  else:
    return a + sum(a - 1)


# O(n * log2n) quazi linear, binary sort

# O(n^2) square
for n in range(1,10):
  for n in range(1,10):
    pass

# O(n^x) polinomic

# O(c^n) exponet

# O(n!) factorial

